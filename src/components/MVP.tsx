import React from "react";
import { Button } from "createistic-designsystem";

const MVP = (): React.ReactElement => {
  return (
    <div>
      <h1>Browse Sprints</h1>
      <Button type="primary" text="Get verified" />
    </div>
  );
};

export default MVP;
